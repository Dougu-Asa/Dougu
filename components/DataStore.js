import { DataStore } from 'aws-amplify';
import { User } from '../src/models';

// create a class which handles amplify datastorage functions

export default class DataStore {
    
    createOrgUserStorage = async (organization, user) => {
        
    }

}